import { Component } from '@angular/core';
import { Button } from 'primeng/button';

/**
 * Catálogo Button — componentes PrimeNG v20 reales (<p-button>), no HTML emulado.
 * Variantes alineadas con legacy index.html (tab-button-catalog).
 */
@Component({
  selector: 'app-button-catalog',
  standalone: true,
  imports: [Button],
  templateUrl: './button-catalog.component.html',
  styleUrl: './button-catalog.component.css',
})
export class ButtonCatalogComponent {}
